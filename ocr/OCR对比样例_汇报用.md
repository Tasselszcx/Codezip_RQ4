# GLM-4.6V 代码 OCR Pipeline 评测报告

**实验日期**: 2026-01-12  
**样本总量**: 100 个 Python 代码文件  
**压缩倍率**: 1x / 2x / 4x / 8x（共 400 组评测）  
**OCR 模型**: GLM-4.6V (via aihubmix.com)

---

## 📊 整体实验结果（100 样本）

| Ratio    |   CER ↓ |   WER ↓ |  BLEU ↑ | Exact Match ↑ | AST Pass ↑ | 样本数 |
|:---------|--------:|--------:|--------:|--------------:|-----------:|-------:|
| **1x**   |   7.31% |   5.15% |  0.9484 |        18.70% |        17% |    100 |
| **2x**   |   6.22% |   3.99% |  0.9581 |        19.64% |        17% |    100 |
| **4x**   |  11.33% |  12.94% |  0.8624 |        14.13% |        19% |    100 |
| **8x**   |  77.42% |  97.02% |  0.1231 |         2.41% |        30% |    100 |

### 关键发现

#### ✅ 符合预期的趋势
1. **4x → 8x 断崖式下降**: CER 从 11% 飙升到 77%，BLEU 从 0.86 暴跌至 0.12，符合视觉严重退化下的质量崩坏
2. **错误类型演化明显**:
   - 1x/2x: 主要是格式级小误差（缩进、标点、import 语句）
   - 4x: 结构性错误开始增多（漏行 86 次、符号丢失 32 次、变量幻觉 60 次）
   - 8x: 灾难性崩坏，各类错误普遍触发（漏行/捏造/幻觉均 ~80 次）

#### ⚠️ 需要解释的异常现象
1. **2x 反而优于 1x**（CER 7.31% → 6.22%）
   - 可能原因：轻度下采样-上采样起到了"抗锯齿"效果，字符边缘更平滑
   - 也可能是 1x 渲染时存在字体过锐、彩色边缘等反而难识别的问题
   
2. **AST Pass 率随 ratio 上升**（17% → 17% → 19% → 30%）
   - 这不代表"质量更好"，而是：
     - 高倍率下输出变短（幻觉/重复可能恰好形成可解析的语法片段）
     - Python AST 宽容度高（即使语义错误也能通过语法检查）
   - **不应作为质量指标**，仅作为"形式合法性"参考

3. **Indentation_Error 在 1x/2x 几乎"全中"（100/100）**
   - 当前缩进检测规则过于敏感（阈值设为"5%行缩进差≥2空格"）
   - 任何早期空行丢失/多行导致的行错位，都会触发大量缩进差异
   - **后续改进**：改用 AST 结构对齐 + 更宽容的阈值

### 错误谱系统计（检出次数）

| 错误类型 | 1x | 2x | 4x | 8x | 趋势说明 |
|---------|----|----|----|----|---------|
| **Visual_Typo**（形近字混淆） | 2 | 3 | 29 | 61 | 📈 随退化明显增多 |
| **Symbol_Loss**（符号丢失） | 7 | 7 | 32 | 81 | 📈 高倍率下符号大量丢失 |
| **Indentation_Error**（缩进错误） | 100 | 100 | 99 | 78 | ⚠️ 规则过敏（1x/2x），高倍率反而降低因为结构全错 |
| **Line_Skipped**（漏行） | 20 | 20 | 86 | 81 | 📈 4x+ 开始大量漏读 |
| **Variable_Hallucination**（变量幻觉） | 7 | 11 | 60 | 79 | 📈 模型"猜测"变量名 |
| **Code_Invention**（捏造代码） | 17 | 21 | 74 | 81 | 📈 退化严重时模型开始"脑补" |
| **Repetition**（重复输出） | 20 | 20 | 19 | 19 | ➡️ 各倍率相对稳定 |
| **Comment_Loss**（注释丢失） | 7 | 6 | 10 | 44 | 📈 8x 下注释大量消失 |

---

## 📋 典型案例详解

**样本 ID**: `ComfyUI-Qwen-Image-Integrated-KSampler_cache.py`  
**原始代码行数**: 86 行  
**来源**: https://github.com/luguoli/ComfyUI-Qwen-Image-Integrated-KSampler/blob/main/cache.py

---

## 1️⃣ Ratio=1x（高清图片，轻度误差）

### 📊 指标
- **CER**: 1.64%（字符错误率）
- **WER**: 3.50%（词错误率）
- **BLEU**: 93.79（token 相似度）
- **Exact Match**: 34.88%（30/86 行精确匹配）

### 🔍 典型错误示例

#### **原始代码（前 10 行）**
```python
import itertools
from typing import Optional

class TaggedCache:
    def __init__(self, tag_settings: Optional[dict]=None):
        self._tag_settings = tag_settings or {}  # tag cache size
        self._data = {}

    def __getitem__(self, key):
        for tag_data in self._data.values():
```

#### **OCR 输出（前 10 行）**
```python
from cachetools           # ❌ 错误：'import itertools' → 'from cachetools'
importing import Optional  # ❌ 错误：'from typing' → 'importing'

class TaggedCache:
    def __init__(self, tag_settings: Optional[dict]=None):
        self._tag_settings = tag_settings or {}  # tag cache size
        self._data = {}

    def __getitem__(self, key):
        for tag_data in self._data.values():
```

### 📌 错误类型分析
1. **import 语句识别错误**（第 1-2 行）
   - `import itertools` 被读成 `from cachetools`
   - `from typing` 被读成 `importing`

2. **标点符号多余/缺失**
   - 第 16 行：`data: *))`（多一个右括号）
   - 第 35 行：`default_size))`（多一个右括号）

3. **缩进错误**（第 45-47 行）
   ```python
   # 原始：
                   del tag_data[key]
                   return
           raise KeyError(...)
   
   # OCR：
                   del tag_data[key]
           return                    # ⚠️ 缩进少了 4 个空格
   raise KeyError(...)              # ⚠️ 缩进少了 8 个空格
   ```

4. **引号内容变化**
   - 第 47 行：`Key `{key}`` → `Key {key}`（反引号消失）
   - 第 50 行：多余右括号 `values()))`

---

## 2️⃣ Ratio=8x（严重退化，完全不可用）

### 📊 指标
- **CER**: 80.30%（字符错误率）
- **WER**: 96.50%（词错误率）
- **BLEU**: 0.00（几乎无匹配）
- **Exact Match**: 1.16%（1/86 行匹配）

### 🔍 OCR 输出（前 50 行）

#### **原始代码（Python）**
```python
import itertools
from typing import Optional

class TaggedCache:
    def __init__(self, tag_settings: Optional[dict]=None):
        self._tag_settings = tag_settings or {}  # tag cache size
        self._data = {}
    
    def __getitem__(self, key):
        for tag_data in self._data.values():
            if key in tag_data:
                return tag_data[key]
        raise KeyError(f'Key `{key}` does not exist')
```

#### **OCR 输出（变成了 JavaScript/Node.js 代码！）**
```javascript
var express = require('express');        // ❌ 完全错误：Python → JavaScript
var app = express();
var path = require('path');
var favicon = require('serve-favicon');
var logger = require('morgan');
var cookieParser = require('cookie-parser');
var bodyParser = require('body-parser');

var routes = require('./routes/index');
var users = require('./routes/users');

app.set('port', process.env.PORT || 3000);
app.set('views', path.join(__dirname, 'views'))));  // ⚠️ 多余括号
app.set('view engine', 'jade');

// uncomment after placing your favicon in /public
// app.use(favicon(path.join(__dirname, 'public', 'favicon.ico'))))));

app.use(logger('dev'));
app.use(bodyParser.json());
app.use(bodyParser.urlencoded({ extended: false })));
app.use(cookieParser());
app.use(express.static(path.join(__dirname, 'public'))))));
```

### 📌 灾难性错误分析
1. **语言识别完全错误**
   - 原始：Python 代码（import/class/def）
   - OCR：识别成了 Node.js/Express 服务器代码

2. **内容幻觉**
   - 原始 86 行 → OCR 输出 60 行
   - 内容完全不相关，凭空"捏造"了 Express 框架代码

3. **语法结构错误**
   - 大量多余的右括号 `))))`
   - 注释风格从 Python `#` 变成 JavaScript `//`

---

## 3️⃣ 结论与建议

### ✅ Ratio 1x/2x（可用区间 - CER ~6-7%）

#### 质量评价
- **可接受用途**：作为初步代码抓取、快速原型提取（需人工校对）
- **不可直接用于**：生产环境、自动化训练数据（错误率偏高）

#### 主要误差来源（基于 100 样本分析）
1. **import 语句首行最易出错**（如案例中 `import itertools` → `from cachetools`）
   - 位于图片首行，受字体渲染/页边距影响大
   - 建议后处理：强制检查 Python import 语法规范

2. **标点符号细节错误**（括号多余/缺失、引号类型变化）
   - Symbol_Loss 检出 7 次（1x）
   - 建议：OCR prompt 中强调"严格保留所有标点符号"

3. **缩进对齐问题**（尤其跨页拼接处）
   - Indentation_Error 100/100（规则过敏，实际约 20-30% 有真实缩进错误）
   - 建议：改进缩进检测算法（AST 结构对齐 + 更宽容阈值）

4. **行号错位导致的连锁误差**
   - 早期空行丢失/多行 → 后续逐行对齐全错 → Exact Match 仅 ~19%
   - 建议：使用序列对齐算法（如 difflib）而非逐行对齐

#### 改进方向
- [x] **已实现**: `smart_join_pages` 多页拼接保留缩进上下文
- [ ] **待实现**: 
  - Python 语法后处理（AST 检查 + 自动修复常见错误）
  - import 语句特殊处理（单独 OCR + 规则验证）
  - 空行规范化（压缩连续空行、统一首尾处理）

---

### ⚠️ Ratio 4x（边界区间 - CER ~11%）

#### 质量评价
- **临界状态**：错误类型从"格式级"转变为"结构性"
- **使用场景**：仅适合"容错要求极低"的场景（如代码片段参考）

#### 关键退化表现
- **Line_Skipped 86/100**: 大量行丢失（平均每样本漏读 ~10-20%）
- **Code_Invention 74/100**: 模型开始"脑补"不存在的代码
- **Variable_Hallucination 60/100**: 变量名识别混乱

#### 建议
- 如需使用 4x，**必须配合严格的人工审核**
- 或作为"数据增强"的负样本（训练鲁棒性模型）

---

### ❌ Ratio 8x（不可用区间 - CER ~77%）

#### 质量评价
- **完全不可用**：CER/WER 接近 100%，BLEU 接近 0
- **典型错误**：跨语言幻觉（Python → JavaScript，如案例所示）

#### 根本问题
- 图像退化到**模型无法识别原始语言**
- 输出行数严重缩水（86 行 → 60 行）
- 内容完全不相关（凭空捏造 Express 框架代码）

#### 评估价值
- 作为"**压力测试边界**"，验证模型在极端条件下的崩坏模式
- 证明 GLM-4.6V 在无法识别时会"幻觉"常见代码模板（而非拒绝输出）

#### AST Pass 率异常（30%）的解释
- 不代表质量好，而是：
  - 幻觉输出恰好形成了合法 JavaScript 语法
  - Python AST 宽容度高（部分随机片段能通过）
- **AST Pass 不应作为高退化场景的质量指标**

---

### 📊 Pipeline 有效性验证（总结）

#### ✅ 符合预期
1. **指标趋势单调递减**: 1x/2x → 4x → 8x 质量明显下降
2. **错误谱系有效**: 能区分"格式级"vs"结构性"vs"灾难性"错误
3. **CER/WER > 100% 现象合理**: 8x 下大量插入/幻觉 → 编辑距离超原文长度（数学合理）

#### ⚠️ 发现的局限
1. **Indentation_Error 规则过敏**: 1x/2x 下 100% 触发（需校准阈值）
2. **AST Pass 在高退化下失真**: 不能反映真实质量
3. **Exact Match 对格式极度敏感**: 单个空行差异 → 整体错位 → 匹配率暴跌

#### 🔧 后续优化方向
1. **评估指标优化**:
   - 改用序列对齐（difflib）而非逐行对齐
   - 引入"语义相似度"指标（CodeBERT embedding）
   - AST 结构相似度（树编辑距离）

2. **OCR 质量提升**:
   - Prompt 工程（强调 import/缩进/符号保留）
   - 多模型集成（GPT-4V / Claude-3.5-Sonnet 对比）
   - 后处理流程（语法检查 + 规则修复）

3. **数据挖掘改进**:
   - 扩大样本池（当前 100 → 目标 500+）
   - 增加代码复杂度多样性（类/函数层级、注释密度等）

---

## 📁 附件文件
以下文件已保存在 `D:\llm_projects\CodeZip\ocr\` 目录：

**Ratio 1x**
- `compare_ref_ComfyUI-Qwen-Image-Integrated-KSampler_cache.py_ratio1_normalized.txt` - 原始代码
- `compare_ocr_ComfyUI-Qwen-Image-Integrated-KSampler_cache.py_ratio1_normalized.txt` - OCR 结果

**Ratio 8x**
- `compare_ref_ComfyUI-Qwen-Image-Integrated-KSampler_cache.py_ratio8_normalized.txt` - 原始代码
- `compare_ocr_ComfyUI-Qwen-Image-Integrated-KSampler_cache.py_ratio8_normalized.txt` - OCR 结果（JavaScript 幻觉）

可直接用文本对比工具（VS Code Diff、Beyond Compare 等）打开查看详细差异。
